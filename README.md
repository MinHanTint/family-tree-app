# family-tree-app
record information about a family tree by allowing user to use graphical user interface (GUI) which contains functions to create, save, load, add member and update member information
